name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pywin32

      - name: Build executable
        run: pyinstaller build.spec

      - name: Build standalone Python script
        run: python build_standalone.py

      - name: Copy VBS scripts to dist
        run: |
          copy VBS_vision\xls2xlsx.vbs dist\
          copy VBS_vision\xls2xlsx_en.vbs dist\

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/xls2xlsx.exe
            dist/xls2xlsx.py
            dist/xls2xlsx.vbs
            dist/xls2xlsx_en.vbs
          generate_release_notes: true
